[pytest]
testpaths =
    backend/tests
    intelligence_agent/test
    tests/contract
    tests/smoke
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (mock servers or test HTTP)
    contract: Cross-service contract tests (proto definitions)
    smoke: Docker Compose infrastructure smoke tests
    e2e: End-to-end tests (full system)
